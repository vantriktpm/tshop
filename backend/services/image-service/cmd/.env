DB_DSN=host=localhost user=postgres password=1 dbname=postgres port=5432 sslmode=disable

# MinIO configuration
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# Kafka & Redis hosts for image-service
KAFKA_BROKER=localhost:9092
REDIS_ADDR=localhost:6379

